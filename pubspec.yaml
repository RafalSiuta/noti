name: noti_2
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.0 # import 'package:google_fonts/google_fonts.dart';
  table_calendar: ^3.0.9
  flutter_time_picker_spinner: ^2.0.0 #import 'package:flutter_time_picker_spinner/flutter_time_picker_spinner.dart';
  provider: ^6.0.5 #https://pub.dev/packages/provider # import 'package:provider/provider.dart';
  shared_preferences: ^2.0.20 #https://pub.dev/packages/shared_preferences/install import 'package:shared_preferences/shared_preferences.dart';
  csshadow: ^0.0.2  #https://pub.dev/packages/csshadow/install import 'package:csshadow/csshadow.dart';
  share_plus: ^6.3.1 #https://pub.dev/packages/share_plus/install import 'package:share_plus/share_plus.dart';
  flutter_native_timezone: ^2.0.0 #import 'package:flutter_native_timezone/flutter_native_timezone.dart';
  carousel_slider: ^4.2.1 #https://pub.dev/packages/carousel_slider/install import 'package:carousel_slider/carousel_slider.dart';
  font_awesome_flutter: ^10.4.0 #import 'package:font_awesome_flutter/font_awesome_flutter.dart';
  intl: ^0.18.0 #import 'package:intl/intl.dart';
  flutter_staggered_animations: ^1.1.1 #import 'package:flutter_staggered_animations/flutter_staggered_animations.dart';
  simple_animations: ^5.0.2 #import 'package:simple_animations/simple_animations.dart';
  animations: ^2.0.7 #import 'package:animations/animations.dart';
  hive: ^2.2.3 #import 'package:hive/hive.dart';
  hive_flutter: ^1.1.0 #import 'package:hive_flutter/hive_flutter.dart';
  photo_manager: ^2.6.0 #import 'package:photo_manager/photo_manager.dart';
  camera: ^0.10.5+2 #import 'package:camera/camera.dart';

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
